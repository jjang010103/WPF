<UserControl x:Class="MEMO.UcChildView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MEMO"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="400" Name="ucThis">
    <DockPanel LastChildFill="True">
        <Grid Height="21" DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="50"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="btnLoad" Content="Load" Grid.Column="0" Click="btnLoad_Click"/>
            <Button x:Name="btnSave" Content="Save" Grid.Column="1" Click="btnSave_Click"/>
        </Grid>
        <Label Content="{Binding Path=FilePath, ElementName=ucThis}" DockPanel.Dock="Bottom"/>
        <TextBox Text="{Binding Path=ContentValue, ElementName=ucThis}"/>
    </DockPanel>
</UserControl>
